description: Невірний формат запиту
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Something went wrong
        data:
          type: string
          example: is not a valid id